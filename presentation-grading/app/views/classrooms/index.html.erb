<div class="col-xs-12 col-xs-offset-0 col-md-6 col-md-offset-3">
  <h1>Choose Your Class</h1>
  <% @classrooms.each do |classroom| %>
    <%= link_to classroom.name, classroom_path(classroom), class: 'btn btn-primary btn-large' %>
  <% end %>
</div>
